{
  "build": { 
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "workshop-user",
  "workspaceFolder": "/workspaces/imd-workshop-2025",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"],
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/envs/workshop/bin/python",
        "terminal.integrated.shell.linux": "/bin/zsh"
      }
    }
  },
  "postCreateCommand": "git config --global --add safe.directory /workspaces/imd-workshop-2025",
  "postStartCommand": "/bin/zsh -c '. /opt/conda/etc/profile.d/conda.sh && conda activate workshop && python -m ipykernel install --user --name workshop --display-name \"workshop\"'"
}
